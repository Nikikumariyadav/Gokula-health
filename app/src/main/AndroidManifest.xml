<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <application
        android:name=".GokulaApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GokulaHealth">

        <activity
            android:name=".ui.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.GokulaHealth.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.MainActivity" android:exported="false"
            android:theme="@style/Theme.GokulaHealth.ActionBar"/>
        <activity android:name=".ui.cattle.AddCattleActivity" android:exported="false"
            android:theme="@style/Theme.GokulaHealth.ActionBar"/>
        <activity android:name=".ui.cattle.CattleDetailActivity" android:exported="false"
            android:theme="@style/Theme.GokulaHealth.ActionBar"/>
        <activity android:name=".ui.milk.AddMilkActivity" android:exported="false"
            android:theme="@style/Theme.GokulaHealth.ActionBar"/>
        <activity android:name=".ui.vaccination.AddVaccinationActivity" android:exported="false"
            android:theme="@style/Theme.GokulaHealth.ActionBar"/>

        <receiver android:name=".notification.VaccinationReceiver" android:exported="false" />

        <receiver android:name=".notification.BootReceiver" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
